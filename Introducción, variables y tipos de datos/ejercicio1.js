//creamos las variables, recomendamos siempre que puedan crearla con const y no let (salvo que la vayan a modificar posteriormente)
const edad;
const peso;

//imprimimos las dos variables y se nos muestra por consola
console.log(edad);
console.log(peso);